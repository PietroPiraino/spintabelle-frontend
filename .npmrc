# @angular/ssr + @angular/platform-server hanno peer-dep pinnati esatti che
# npm 7+ risolve in modo troppo stretto (ERESOLVE) sul mix di patch ^22.0.0.
# legacy-peer-deps allinea l'install locale e quello di Cloudflare Pages (npm ci).
legacy-peer-deps=true
